{{- if .Values.processor.image.imageCredentials -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rating.processor.pullSecretName" . }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ printf "{\"auths\": {\"%s\": {\"auth\": \"%s\"}}}" .Values.processor.image.imageCredentials.registry (printf "%s:%s" .Values.processor.image.imageCredentials.username .Values.processor.image.imageCredentials.password | b64enc) | b64enc }}
{{- end -}}
